# ─────────────────────────────────────────────────────────────────────────
# Gaps — Tomada de Decisão · variáveis de ambiente (Vercel → Settings → Env)
# Copie para .env para rodar local (vercel dev). NUNCA versione o .env real.
# Use ASPAS SIMPLES no username (a barra invertida quebra dotenv com "").
# ─────────────────────────────────────────────────────────────────────────

# Postgres do Supabase — use a connection string do POOLER (porta 6543)
# Supabase → Project → Settings → Database → Connection string → "Transaction pooler"
# Formato: postgresql://postgres.kpimalwnswxalwbidkog:SENHA@aws-...pooler.supabase.com:6543/postgres
DATABASE_URL='postgresql://postgres.kpimalwnswxalwbidkog:SUA_SENHA_DB@aws-0-us-east-2.pooler.supabase.com:6543/postgres'

# Tasks SC (OAuth2 Password Grant) — api.tscst.com.br
TASKS_USERNAME='actvs\reges.werlang'
TASKS_PASSWORD='sua_senha_do_AD'
TASKS_SC_BASE_URL='https://api.tscst.com.br/restAPI'

# Segredo para assinar o cookie de sessão (gere um aleatório longo)
#   python3 -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET='troque_por_um_hex_de_64_chars'

# (opcional) parâmetros scrypt do login. Deixe em branco para auto-detectar.
# SCRYPT_N=16384
# SCRYPT_R=8
# SCRYPT_P=1
